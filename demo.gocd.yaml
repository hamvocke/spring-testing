pipelines:
    hello-world:
        group: example
        materials:
            hello-world-git:
                git: https://github.com/hamvocke/spring-testing.git
        stages:
            - build-and-test:
                secure_variables:
                    WEATHER_API_KEY: "mC8mFRtIhv0sVwT4r2SJvuf7m2p+A1nm6gZHsLkrdgLqKfUQkrDkbg=="
                jobs:
                    build:
                        tasks:
                            - exec:
                                command: ./gradlew
                                arguments:
                                    - build

            - deploy-int:
                jobs:
                    deploy:
                        tasks:
                            - exec:
                                command: echo
                                arguments:
                                    - "Deploying to Int"
                    smoke-test:
                        tasks:
                            - exec:
                                command: echo
                                arguments:
                                    - "Running smoke tests"

            - end-to-end-test:
                jobs:
                    deploy:
                        tasks:
                            - exec:
                                command: echo
                                arguments:
                                    - "Running end to end tests"
